configure_file(input: 'catalog.in',
               output: 'catalog',
               configuration: yelp_xsl_conf,
               install: true,
               install_dir: join_paths(yelp_datadir, 'dtd'))
